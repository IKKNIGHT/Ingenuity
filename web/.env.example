# Optional. Production: PartyKit host, e.g. ingenuity.yourname.partykit.dev (no https://).
# Local dev: leave unset — client uses 127.0.0.1:1999 (same as `partykit dev -p 1999` in npm run dev:full).
# VITE_PARTYKIT_HOST=ingenuity.ikknight.partykit.dev
